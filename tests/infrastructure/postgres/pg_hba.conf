# PostgreSQL Client Authentication Configuration File
# For pygwire functional testing

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Authentication test users with specific methods
host    all             trust_user      0.0.0.0/0               trust
host    all             md5_user        0.0.0.0/0               md5
host    all             scram_user      0.0.0.0/0               scram-sha-256

# Default postgres user - trust for easy testing
host    all             postgres        0.0.0.0/0               trust

# IPv6 versions
host    all             trust_user      ::/0                    trust
host    all             md5_user        ::/0                    md5
host    all             scram_user      ::/0                    scram-sha-256
host    all             postgres        ::/0                    trust

# Local connections
local   all             all                                     trust
